---
"@jmfederico/pi-web": minor
---

Add the inline Review card: write/edit tool rows now render their captured review record (file path, added/modified/deleted badge, add/delete counts, expandable hunks) with a guarded Rollback action (confirm prompt, conflict/unavailable feedback, rolled-back state persisted across reloads).

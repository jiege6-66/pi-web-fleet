---
"@jmfederico/pi-web": minor
---

Give browser plugins a host translation channel: `PluginI18n` on the activation context and runtime/workspace contexts, `titleKey`/`descriptionKey`/`groupKey` for contribution metadata, and merged per-plugin locale fragments. Bundled plugin panels (Files and the rest, converted next) can now follow the interface language.
